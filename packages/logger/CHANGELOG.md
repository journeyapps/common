# @journeyapps/common-logger

## 1.1.0

### Minor Changes

- f09187d: Add dynamically configurable hierarchical loggers through `childLogger()`, correct severity filtering, add an `OFF` level, and support transport-aware styled values such as `Log.red()` and `Log.green()` in browser and Node console transports without changing existing variadic logging calls.

## 1.0.1

### Patch Changes

- 6938e8a: Bump all dependencies
